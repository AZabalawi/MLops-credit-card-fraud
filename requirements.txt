# Full project stack: Phase 1 pipeline (DVC + MLflow + scikit-learn) plus the
# Phase 2 serving and monitoring libraries.
#
# scikit-learn note: Phase 1 was pinned to 1.8.0, which requires Python 3.11+.
# Phase 2 pins 1.7.2 so the whole project runs on Python 3.10 as well and, more
# importantly, so the version that fits models/model.pkl is the same one the
# container installs to load it. Re-running the pipeline on 1.7.2 reproduces
# the Phase 1 evaluation metrics exactly (see README section 6).

# --- Phase 1: pipeline, tracking, training ---
dvc==3.67.1
mlflow==3.14.0
scikit-learn==1.7.2
pandas==2.3.3
numpy==2.2.6
matplotlib>=3.8.0
seaborn>=0.13.0
joblib==1.5.3
PyYAML>=6.0.0

# --- Phase 2: API ---
fastapi==0.141.1
uvicorn==0.52.1
pydantic==2.13.4

# --- Phase 2: monitoring ---
evidently==0.7.21
